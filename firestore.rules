rules_version = '2';
// All data access goes through the backend (Admin SDK). Clients never talk to Firestore directly.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
